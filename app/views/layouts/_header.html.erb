<ul class="nav navbar-nav">
   <li class="navbar-text">FlatIron Store</li>
   <li class="active"><%= link_to "Store Home", store_path, class: "navbar-brand" %> </li>
   <li class="dropdown">
     <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories <span class="caret"></span></a>
     <ul class="dropdown-menu">
       <% Category.all.each do |cat| %>
         <li><%= link_to cat.title, category_path(cat) %>
       <% end %>
    </ul>
  </li>
   <% if !!user_signed_in? %>
   <li class="navbar-text">Signed in as <%= current_user.email %></li>
   <li><%= link_to "Sign out", destroy_user_session_url, method: :delete %></li>
   <% else %>
   <li><%= link_to "Sign up", new_user_registration_path %></li>
   <li><%= link_to "sign in", new_user_session_path %></li>
   <% end %>
 
   <% if current_user && current_user.current_cart %>
   <li> <%= link_to "Cart", cart_path(current_user.current_cart) %></li>
   <% end %>